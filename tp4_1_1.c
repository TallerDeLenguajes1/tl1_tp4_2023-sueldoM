#include <stdio.h>
#include <stdlib.h>

int main() {
    int cantidadTareas;

    printf("Ingrese la cantidad de tareas a cargar: ");
    scanf("%d", &cantidadTareas);

    printf("Se cargarán %d tareas.\n", cantidadTareas);

    return 0;
}
